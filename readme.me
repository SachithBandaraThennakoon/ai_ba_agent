python -m venv venv
venv\Scripts\activate

pip install -r requirements.txt


python -m vector_db.load_company_docs

////////////////////////////////////////
🔥 Step 1 — Delete the Vector Store Folder
Close your app first.
Then delete this folder:
vector_db_store/
(Just delete it manually from File Explorer)
This removes all old embeddings.

🔁 Step 2 — Re-ingest Documents
Open a Python shell in project root:
python
Then run:

python

from vector_db.company_knowledge import load_company_docs
load_company_docs("company_docs")

You should see:
✅ Company knowledge loaded into Vector DB

Exit Python:
exit()

▶ Step 3 — Run App Again
python app.py

//////////

openai>=1.0.0
python-dotenv

beautifulsoup4
requests
faiss-cpu
tiktoken
chromadb

langgraph
langchain-core

reportlab

fastapi
uvicorn
pydantic

streamlit
requests
/////////
import os
import chromadb
from chromadb.config import Settings
from openai import OpenAI
from dotenv import load_dotenv
import uuid

load_dotenv()
client = OpenAI()

/////////////

## 🔹 STEP 1 — Deactivate Virtual Environment

```bash
deactivate
```
## 🔹 STEP 2 — Delete the Existing Virtual Environment
```bash
rm -rf venv
```
⚠️ This deletes **only** the virtual environment, **not your code**.
---
## 🔹 STEP 3 — Create a Fresh Virtual Environment (Python 3.11)
```bash
python3.11 -m venv venv
```
## 🔹 STEP 4 — Activate It

```bash
source venv/bin/activate
```
Verify:
```bash
python --version
```
Expected:
```
Python 3.11.x

## 🔹 STEP 5 — Install Clean Dependencies

Make sure you already pulled latest GitHub changes.

Then:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```
## 🔹 STEP 6 — Verify Core Packages
pip show fastapi chromadb langgraph
No errors → ✅ perfect

---
////////////////
STEP 2 — Pull Latest Changes from GitHub
git pull origin main
/////////
python3.11 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
